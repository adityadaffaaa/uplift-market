import React from "react";

const PengaturanAkunLayout = ({ children }) => (
  <div className="h-full grid place-items-center py-20 lg:bg-greyBackground">
    {children}
  </div>
);

export default PengaturanAkunLayout;
