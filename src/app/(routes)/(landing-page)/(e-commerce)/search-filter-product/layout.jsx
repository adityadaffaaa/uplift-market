import React from "react";

const SearchFilterProductLayout = ({ children }) => {
  return (
    <div className="flex border-t-2 mt-24">{children}</div>
  );
};

export default SearchFilterProductLayout;
